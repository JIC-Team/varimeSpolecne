{block content}
{if $user->isLoggedIn()}

<span class="icon user">{$user->getIdentity()->first_name} {$user->getIdentity()->last_name}</span> |
<a n:href="signOut!">Odhlásit se</a>

{else}

<h1 n:block=title>Vařím pro tebe</h1>

<div>
  <a n:href="Sign:in">Přihlásit se</a>
  <small>nebo</small>
  <a n:href="Sign:up">Registrovat</a>
</div>

{/if}
{/block}
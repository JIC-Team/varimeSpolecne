{block content}
{if $user->isLoggedIn()}
	<ul>
		{foreach $attendees as $attendee}
			<li>{$attendee->user->first_name} se chce účastnit <a n:href="Event:view $attendee->event->id">{$attendee->event->title}</a></li>
		{/foreach}
	</ul>
{else}
	<a n:href="Sign:in">Přihlásit se</a>
{/if}
{/block}